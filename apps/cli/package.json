{
  "name": "vmr-ai",
  "type": "module",
  "bin": {
    "vmr": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vmr/core": "workspace:*"
  }
}
