{
  "name": "vmr-ai",
  "type": "module",
  "bin": {
    "vmr": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.2.0",
    "@vmr/core": "workspace:*",
    "cli-progress": "^3.12.0",
    "commander": "^14.0.3"
  }
}
